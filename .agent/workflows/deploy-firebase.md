---
description: Build and deploy the platform to the live Firebase project
---

# // turbo-all
1. Verify being on the correct branch and sync with remote.
2. Run build verification: `npm run build`.
3. Select the correct firebase project: `firebase use bemobile-be-live`.
4. Run full deployment: `firebase deploy`.
5. Verify site status after deployment.
